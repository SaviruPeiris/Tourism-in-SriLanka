const scrollDown = document.querySelector(".explore");

scrollDown.onclick = ()=>{
    window.scrollTo(0,1200);
}

const new_Section = document.getElementById("newSection");

const page_height = document.documentElement.scrollHeight;

// window.addEventListener("scroll",function){
//     const
// }
